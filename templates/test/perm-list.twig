<style type="text/css">
*{ font-size: 25px;}
</style>
{% if perms %}
<table border="1" cellpadding="0" cellspacing="0">
    <tr>
        <th>id</th>
        <th>name</th>
        <th>created_at</th>
    </tr>
    {% for perm in perms %}
    <tr>
        <td>{{ perm.route_name }}</td>
        <td>{{ perm.op_class }}</td>
        <td>{{ perm.op_name }}</td>
        <td>
            <a href="{{ path_for('api_delete_node', {id: node.id}) }}">&nbsp;&lt;&lt;delete</a>
        </td>
    </tr>
    {% endfor %}
    </table>
{% else %}
    <h1>empty</h1>
{% endif %}